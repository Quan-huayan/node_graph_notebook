name: core_data
version: 0.1.0
description: >-
  Pure data model contracts (Node / Concept / Graph / UIStateStore / Hook)
  for the node graph notebook rewrite. Zero platform dependencies
  (no dart:ui / flutter imports).
publish_to: 'none'
resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies: {}

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
